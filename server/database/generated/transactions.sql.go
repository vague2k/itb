// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: transactions.sql

package db

import (
	"context"

	models "itb.ihatedoing.work/server/database/models"
)

const createTransaction = `-- name: CreateTransaction :one
INSERT INTO transactions (category_id, amount_cents, note)
VALUES (?1, ?2, ?3)
RETURNING id, category_id, amount_cents, note, created_at
`

type CreateTransactionParams struct {
	CategoryID  int64
	AmountCents int64
	Note        string
}

func (q *Queries) CreateTransaction(ctx context.Context, arg CreateTransactionParams) (models.Transaction, error) {
	row := q.db.QueryRowContext(ctx, createTransaction, arg.CategoryID, arg.AmountCents, arg.Note)
	var i models.Transaction
	err := row.Scan(
		&i.ID,
		&i.CategoryID,
		&i.AmountCents,
		&i.Note,
		&i.CreatedAt,
	)
	return i, err
}

const listTransactionsByCategory = `-- name: ListTransactionsByCategory :many
SELECT id, category_id, amount_cents, note, created_at FROM transactions
WHERE category_id = ?1
ORDER BY created_at DESC, id DESC
`

func (q *Queries) ListTransactionsByCategory(ctx context.Context, categoryID int64) ([]models.Transaction, error) {
	rows, err := q.db.QueryContext(ctx, listTransactionsByCategory, categoryID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []models.Transaction
	for rows.Next() {
		var i models.Transaction
		if err := rows.Scan(
			&i.ID,
			&i.CategoryID,
			&i.AmountCents,
			&i.Note,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
