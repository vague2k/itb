package components

// Scripts renders the one script tag that loads the component JS bundle.
// Render it once in the layout <head>.
templ Scripts() {
	<script defer nonce={ templ.GetNonce(ctx) } src={ scriptsSrc() }></script>
}
